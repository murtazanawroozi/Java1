
package chapter2sumary;
import java.util. Scanner;

public class Chapter2Sumary {

     public static void main(String[] args) {
         
        /* 
         // 14:(Health application: coputing BMI)
         Scanner input = new Scanner(System.in);
         System.out.println("Enter the height in meter:");
         double height = input.nextDouble();
         System.out.println("Enter the wight in killogram:");
         double weight = input.nextDouble();
         double BMI = weight/(height*height);
         System.out.println("Your BMI is " + BMI);
      */
        
        /*
        // 15:(Geometry:distance of two points)
        Scanner input = new Scanner(System.in);
         System.out.println("Enter the X1 and Y1:");
         double x1 = input.nextDouble();
         double y1 = input.nextDouble();
         System.out.println("Enter the X2 and Y2:");
         double x2 = input.nextDouble();
         double y2 = input.nextDouble();
         double a = x2 - x1;
         double b = y2 - y1;
         double c = (a*a)+(b*b);
         System.out.println("The distance between two point is " + Math.pow(c,0.5));
           */
        
        /*
        // 16:(Geometry:area of hixagon)
        Scanner input = new Scanner(System.in);
         System.out.println("Enter the side of hixaon: ");
         double s = input.nextDouble();
       double area = (5.196/2)*(s*s);
         System.out.println("The area of hixagon is " + area);
        
                  */  
        
        /*
        
        // 17: (sceince:wind_chill temperture)
        
        Scanner input = new Scanner(System.in);
        
        
        
        
        
        
        */
        
        /*
        //18: (print a table)
         System.out.println((int)Math.pow(5,6));
         System.out.println((int) Math.pow(4,5));
         System.out.println((int) Math.pow(3,4));
         System.out.println((int) Math.pow(2,3));
           */
        
        /*
        //19:(Geometry:area of triangle)
        Scanner input = new Scanner(System.in);
         System.out.println("Enter the X1 and Y1:");
         double x1 = input.nextDouble();
         double y1 = input.nextDouble();
         System.out.println("Enter the X2 and Y2:");
         double x2 = input.nextDouble();
         double y2 = input.nextDouble();
         System.out.println("Enter the X3 and Y3:");
         double x3 = input.nextDouble();
         double y3 = input.nextDouble();
         double area = 0.5*(x1*(y2-y3)+x2*(y3-y1)+x3*(y1-y2)); 
         System.out.println("The area is = " + area);
        */
        // 20
                 
                 
                 
    }
    
}
